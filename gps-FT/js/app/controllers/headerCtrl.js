materialAdmin
    .controller('headerCtrl', headerCtrl);

headerCtrl.$inject = [
    "$localStorage",
    "$rootScope",
    "$scope"
];

function headerCtrl(
    $localStorage,
    $rootScope,
    $scope

) {
    let vm = this;

}